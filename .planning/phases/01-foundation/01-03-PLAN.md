---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - dashboard/render.yaml
  - dashboard/server.js
autonomous: false

requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-05

user_setup:
  - service: supabase
    why: "PostgreSQL database — Render free tier MUST NOT be used (D-12)"
    env_vars:
      - name: DATABASE_URL
        source: "Supabase Dashboard -> Project Settings -> Database -> Connection string (URI)"
    dashboard_config:
      - task: "Create a new Supabase project (free tier)"
        location: "https://supabase.com/dashboard -> New Project"
  - service: render
    why: "Web service hosting for the Express dashboard"
    env_vars:
      - name: SESSION_SECRET
        source: "Generate with: node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\""
      - name: DASHBOARD_PASSWORD_HASH
        source: "Generate with: node -e \"console.log(require('bcryptjs').hashSync('your-chosen-password', 10))\""
      - name: NODE_ENV
        source: "Set to: production"
    dashboard_config:
      - task: "Create a new Web Service connected to the repo"
        location: "Render Dashboard -> New Web Service"

must_haves:
  truths:
    - "Visiting the Render URL shows the login page"
    - "Entering the correct password shows the dashboard with seed data counts > 0"
    - "GET /health returns 200 with db: connected"
    - "The database is NOT Render free PostgreSQL — it is Supabase free tier or Render Starter"
    - "Layout renders without horizontal scroll at 375px"
  artifacts:
    - path: "dashboard/render.yaml"
      provides: "Render deployment configuration"
      contains: "buildCommand"
  key_links:
    - from: "Render Web Service"
      to: "Supabase PostgreSQL"
      via: "DATABASE_URL env var"
      pattern: "DATABASE_URL"
---

<objective>
Deploy the dashboard to Render, provision the database, and verify the complete end-to-end flow.

Purpose: Make the dashboard live and accessible — completing Phase 1 by confirming login, seed data, health check, and mobile layout all work in production.
Output: Live dashboard URL, confirmed DB tier, all Phase 1 success criteria met.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Render deployment config and prepare for deploy</name>
  <files>
    dashboard/render.yaml
  </files>
  <read_first>
    dashboard/package.json,
    dashboard/server.js,
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
**dashboard/render.yaml** — Render Blueprint for the web service:

```yaml
services:
  - type: web
    name: social-dashboard
    runtime: node
    region: frankfurt
    plan: free
    buildCommand: cd dashboard && npm install && npm run build:css
    startCommand: cd dashboard && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: SESSION_SECRET
        sync: false
      - key: DASHBOARD_PASSWORD_HASH
        sync: false
    healthCheckPath: /health
```

Note: `region: frankfurt` for EU proximity (Troyes, France). The `plan: free` is for the web service (not the database). Database is provisioned separately on Supabase per D-12.

Also verify dashboard/server.js has the Render build command working correctly: `npm install && npm run build:css` must succeed, and `node server.js` must start listening.
  </action>
  <verify>
    <automated>cd "E:/Site CL/dashboard" && node -e "const y=require('js-yaml'); const f=require('fs').readFileSync('render.yaml','utf8'); const c=y.load(f); console.log('has buildCommand:', !!c.services[0].buildCommand); console.log('has healthCheck:', !!c.services[0].healthCheckPath)"</automated>
  </verify>
  <acceptance_criteria>
    - dashboard/render.yaml contains `buildCommand:` with `npm install && npm run build:css`
    - dashboard/render.yaml contains `startCommand:` with `node server.js`
    - dashboard/render.yaml contains `healthCheckPath: /health`
    - dashboard/render.yaml contains `DATABASE_URL` and `SESSION_SECRET` and `DASHBOARD_PASSWORD_HASH` in envVars
  </acceptance_criteria>
  <done>Render deployment config ready. Build and start commands verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Provision DB + Deploy + Verify end-to-end</name>
  <files>dashboard/render.yaml</files>
  <action>
This is a human verification checkpoint. The executor presents the setup and verification steps to the user and waits for approval.

Pre-deploy setup (one-time):
1. Provision Supabase free tier: https://supabase.com/dashboard -> New Project -> copy the connection string (URI format)
2. Generate session secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
3. Generate password hash: cd dashboard && node -e "console.log(require('bcryptjs').hashSync('your-chosen-password', 10))"
4. On Render: create Web Service from this repo, set root directory to dashboard/
5. Set env vars: DATABASE_URL (from Supabase), SESSION_SECRET, DASHBOARD_PASSWORD_HASH, NODE_ENV=production
  </action>
  <what-built>
Complete Phase 1 dashboard: Express server with password auth, sidebar nav shell, health status card, idempotent YAML seed, responsive mobile layout.
  </what-built>
  <how-to-verify>
**Verify (5 checks matching Phase 1 success criteria):**
1. Visit `https://[your-render-url].onrender.com` -> should show branded login page with "Social Dashboard / Corinne Lacoste" and password field
2. Enter wrong password -> should show "Mot de passe incorrect. Réessayez." in red
3. Enter correct password -> should redirect to dashboard showing "Bienvenue, Benjamin", green dot "Base de données connectée", and seed data counts (Posts > 0, Commentaires > 0, Prospects > 0)
4. Visit `https://[your-render-url].onrender.com/health` -> should return JSON with `"status":"ok"`, `"db":"connected"`
5. Open Chrome DevTools -> toggle device toolbar -> set width to 375px -> verify no horizontal scroll on login page AND dashboard page; sidebar should be hidden behind hamburger button
6. Verify Supabase dashboard shows the project is on free tier (NOT Render free PostgreSQL) — confirming D-12

**Sidebar check:** Accueil should be active (white text, left orange border). Posts, Commentaires, Pipeline, Statistiques should be greyed out with "Bientot" badges.
  </how-to-verify>
  <verify>
    <automated>cd "E:/Site CL/dashboard" && node --test tests/*.test.js 2>&1 | tail -3</automated>
  </verify>
  <done>User confirms live dashboard works: login flow, health check, seed data visible, mobile responsive, DB is not Render free tier.</done>
  <resume-signal>Type "approved" with the live URL, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All 5 Phase 1 success criteria from ROADMAP.md:
1. Visiting Render URL shows login page; correct password grants access; wrong password rejected
2. After first boot, all YAML data exists in DB; second boot adds no duplicates
3. GET /health returns 200 with DB connection status
4. Layout renders without horizontal scroll at 375px
5. Render free PostgreSQL is NOT used — Supabase free tier or Render Starter confirmed
</verification>

<success_criteria>
- Live dashboard accessible at Render URL
- Login/logout flow works with password auth
- Health endpoint returns DB connected with seed data counts > 0
- Mobile layout responsive at 375px
- Database is Supabase free tier (not Render free PostgreSQL)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
