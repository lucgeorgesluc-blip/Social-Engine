---
phase: 02-read-layer
plan: 01
type: execute
wave: 1
depends_on: ["01-03"]
files_modified:
  - dashboard/views/layout.ejs
  - dashboard/views/dashboard.ejs
  - dashboard/routes/dashboard.js
  - dashboard/lib/dm-config.js
  - dashboard/tests/phase2/inbox.test.js
  - dashboard/public/css/tailwind.css
autonomous: true
requirements:
  - INBOX-01
  - INBOX-02
  - INBOX-03
  - INBOX-04

must_haves:
  truths:
    - "Homepage shows today's scheduled/published posts with status badges (max 5 + 'Voir tout' link)"
    - "Homepage shows pending comments (max 5 + 'Voir tout' link)"
    - "Homepage shows DM follow-ups due (max 5 + 'Voir tout' link)"
    - "Comments older than 2 hours without response are flagged as overdue"
    - "Overdue items use border-l-4 border-red-500 (left border accent, NOT bg fill) per CONTEXT.md D-04"
    - "Sidebar nav items Posts, Commentaires, Pipeline, Statistiques are active links"
  artifacts:
    - path: "dashboard/views/layout.ejs"
      provides: "Sidebar with all 5 nav items enabled as links"
      contains: "href=\"/posts\""
    - path: "dashboard/views/dashboard.ejs"
      provides: "Priority inbox with 3 sections capped at 5 items, each with 'Voir tout' link, border-l-4 border-red-500 for overdue"
      contains: "border-l-4 border-red-500"
    - path: "dashboard/routes/dashboard.js"
      provides: "Enhanced GET / with 4 SQL queries for inbox data"
      contains: "CURRENT_DATE"
    - path: "dashboard/lib/dm-config.js"
      provides: "DM sequence timing rules loaded from config.yaml with hardcoded fallbacks"
      contains: "getDmSequenceRules"
    - path: "dashboard/tests/phase2/inbox.test.js"
      provides: "Integration tests for homepage inbox sections"
      contains: "describe("
  key_links:
    - from: "dashboard/routes/dashboard.js"
      to: "dashboard/lib/dm-config.js"
      via: "require('../lib/dm-config')"
      pattern: "getDmSequenceRules"
    - from: "dashboard/routes/dashboard.js"
      to: "dashboard/lib/db.js"
      via: "require('../lib/db') for inbox SQL queries"
      pattern: "query.*CURRENT_DATE"
---

<objective>
Activate sidebar navigation and build the priority inbox homepage. Each section caps at 5 items with a "Voir tout" link. Overdue items use a left red border accent (NOT bg fill) per CONTEXT.md D-04.

Purpose: Transform the health-status-only homepage into an actionable priority inbox.
Output: Enhanced homepage with 3 capped inbox sections, active sidebar nav, DM timing helper, integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-read-layer/02-CONTEXT.md
@.planning/phases/02-read-layer/02-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
From dashboard/lib/db.js:
```javascript
const { query } = require('../lib/db');
// query(text, params) -> Promise<{ rows: Array }>
```

From dashboard/routes/auth.js:
```javascript
const { isAuthenticated } = require('./auth');
```

From dashboard/lib/schema.sql (table columns):
```sql
-- posts: id, status, platform, type, hook, published_date, created_at
-- comments: id, post_id, date, author_name, classification, comment_text, response_status, created_at
-- prospects: id, prospect_name, stage, date_first_contact, messages, notes
```

DM sequence rules from .social-engine/config.yaml:
```yaml
dm_sequence:
  message_1_delay: "0h"
  message_2_delay: "48h"
  message_3_delay: "168h"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DM config helper + activate sidebar nav + enhanced homepage route (capped queries)</name>
  <files>
    dashboard/lib/dm-config.js,
    dashboard/views/layout.ejs,
    dashboard/routes/dashboard.js
  </files>
  <read_first>
    dashboard/views/layout.ejs,
    dashboard/routes/dashboard.js,
    dashboard/lib/db.js,
    dashboard/routes/auth.js,
    .social-engine/config.yaml,
    .planning/phases/02-read-layer/02-CONTEXT.md
  </read_first>
  <action>
**dashboard/lib/dm-config.js** — same as before:

```javascript
const yaml = require('js-yaml');
const fs = require('fs');
const path = require('path');

const DEFAULT_RULES = {
  new: { follow_up_after_hours: 0 },
  msg1_sent: { follow_up_after_hours: 48 },
  msg2_sent: { follow_up_after_hours: 168 },
  msg3_sent: { follow_up_after_hours: 336 }
};

let _rules = null;

function getDmSequenceRules() {
  if (_rules) return _rules;
  try {
    const configPath = path.resolve(__dirname, '../../.social-engine/config.yaml');
    const config = yaml.load(fs.readFileSync(configPath, 'utf8'));
    const seq = config.dm_sequence;
    if (seq) {
      _rules = {
        new: { follow_up_after_hours: parseDelay(seq.message_1_delay) || 0 },
        msg1_sent: { follow_up_after_hours: parseDelay(seq.message_2_delay) || 48 },
        msg2_sent: { follow_up_after_hours: parseDelay(seq.message_3_delay) || 168 },
        msg3_sent: { follow_up_after_hours: 336 }
      };
    } else {
      _rules = DEFAULT_RULES;
    }
  } catch (e) {
    console.error('[dm-config] Could not load config.yaml:', e.message);
    _rules = DEFAULT_RULES;
  }
  return _rules;
}

function parseDelay(str) {
  if (!str) return null;
  const match = String(str).match(/^(\d+)h$/);
  return match ? parseInt(match[1], 10) : null;
}

module.exports = { getDmSequenceRules };
```

**dashboard/views/layout.ejs** — Replace 4 disabled `<span>` items with active `<a>` links + `currentPath` highlighting (same as previous version — see prior plan revision for full nav block). Pattern:

```html
<a href="/posts" class="... <%= currentPath === '/posts' || currentPath === '/posts/calendar' ? 'text-white bg-white/10 border-l-4 border-primary' : 'text-white/70 hover:text-white hover:bg-white/5' %>">Posts</a>
```

Repeat for `/comments`, `/pipeline`, `/stats`. Remove `pointer-events-none` and `Bientot` badges.

**dashboard/routes/dashboard.js** — Replace GET / with priority inbox. Cap each section at 5 items via `LIMIT 5` (CONTEXT.md D-04). Keep one extra count query per section for the "Voir tout" badge.

```javascript
const express = require('express');
const router = express.Router();
const { query } = require('../lib/db');
const { isAuthenticated } = require('./auth');
const { getDmSequenceRules } = require('../lib/dm-config');

router.get('/health', async (req, res) => { /* keep existing exactly */ });

router.get('/', isAuthenticated, async (req, res, next) => {
  try {
    // INBOX-01: Today's posts (cap 5)
    const { rows: todayPosts } = await query(`
      SELECT id, hook, status, platform, type, published_date
      FROM posts
      WHERE published_date = CURRENT_DATE
      ORDER BY created_at ASC
      LIMIT 5
    `);
    const { rows: [{ count: todayPostsTotal }] } = await query(
      "SELECT COUNT(*)::int AS count FROM posts WHERE published_date = CURRENT_DATE"
    );

    // INBOX-02: Pending comments (cap 5)
    const { rows: pendingComments } = await query(`
      SELECT c.id, c.author_name, c.comment_text, c.classification,
             c.created_at, c.date, p.hook AS post_hook, p.id AS post_id
      FROM comments c
      LEFT JOIN posts p ON c.post_id = p.id
      WHERE c.response_status = 'pending'
      ORDER BY c.date ASC
      LIMIT 5
    `);
    const { rows: [{ count: pendingCommentsTotal }] } = await query(
      "SELECT COUNT(*)::int AS count FROM comments WHERE response_status = 'pending'"
    );

    // INBOX-04: Flag comments older than 2h
    const TWO_HOURS_MS = 2 * 60 * 60 * 1000;
    const now = Date.now();
    const commentsWithFlags = pendingComments.map(c => ({
      ...c,
      isOverdue: (now - new Date(c.created_at || c.date).getTime()) > TWO_HOURS_MS
    }));

    // INBOX-03: DM follow-ups (cap 5)
    const { rows: activeProspects } = await query(`
      SELECT id, prospect_name, full_name, stage, date_first_contact, notes
      FROM prospects
      WHERE stage NOT IN ('booked', 'converted', 'lost')
      ORDER BY date_first_contact ASC
      LIMIT 5
    `);
    const { rows: [{ count: activeProspectsTotal }] } = await query(
      "SELECT COUNT(*)::int AS count FROM prospects WHERE stage NOT IN ('booked','converted','lost')"
    );

    const rules = getDmSequenceRules();
    const prospectsWithFlags = activeProspects.map(p => {
      const hoursSince = (now - new Date(p.date_first_contact).getTime()) / 3600000;
      const rule = rules[p.stage];
      const dueAfterHours = rule ? rule.follow_up_after_hours : null;
      return {
        ...p,
        isOverdue: dueAfterHours !== null && hoursSince > dueAfterHours,
        hoursSince: Math.floor(hoursSince)
      };
    });

    const { rows: [counts] } = await query(`
      SELECT
        (SELECT COUNT(*) FROM posts) AS posts,
        (SELECT COUNT(*) FROM comments) AS comments,
        (SELECT COUNT(*) FROM prospects) AS prospects
    `);

    res.render('dashboard', {
      currentPath: '/',
      dbConnected: true, dbError: false, counts,
      todayPosts, todayPostsTotal,
      pendingComments: commentsWithFlags,
      pendingCommentsTotal,
      overdueCommentCount: commentsWithFlags.filter(c => c.isOverdue).length,
      activeProspects: prospectsWithFlags,
      activeProspectsTotal,
      overdueProspectCount: prospectsWithFlags.filter(p => p.isOverdue).length
    });
  } catch (err) { next(err); }
});

module.exports = router;
```
  </action>
  <verify>
    <automated>cd "E:/Site CL/dashboard" && node -e "const dm=require('./lib/dm-config'); console.log('rules:', !!dm.getDmSequenceRules().msg1_sent)" && node -e "const fs=require('fs'); const l=fs.readFileSync('views/layout.ejs','utf8'); console.log('posts link:', l.includes('href=\"/posts\"')); console.log('currentPath:', l.includes('currentPath'))" && node -e "const fs=require('fs'); const r=fs.readFileSync('routes/dashboard.js','utf8'); console.log('LIMIT 5:', (r.match(/LIMIT 5/g)||[]).length>=3); console.log('totals:', r.includes('todayPostsTotal'))"</automated>
  </verify>
  <acceptance_criteria>
    - dm-config.js exports `getDmSequenceRules` with DEFAULT_RULES fallback
    - layout.ejs has href to /posts /comments /pipeline /stats; no "Bientot"; uses currentPath
    - dashboard.js queries cap at LIMIT 5 for all 3 inbox sections
    - dashboard.js exposes totals (todayPostsTotal, pendingCommentsTotal, activeProspectsTotal) for "Voir tout" links
    - dashboard.js contains TWO_HOURS_MS for INBOX-04 flag
    - dashboard.js passes currentPath: '/' to render
  </acceptance_criteria>
  <done>DM config helper loads rules. Sidebar links active. Homepage queries return capped (5) inbox items + totals.</done>
</task>

<task type="auto">
  <name>Task 2: Priority inbox view with 5-item cap, "Voir tout" links, border-l-4 border-red-500 overdue style + tests</name>
  <files>
    dashboard/views/dashboard.ejs,
    dashboard/tests/phase2/inbox.test.js,
    dashboard/public/css/tailwind.css
  </files>
  <read_first>
    dashboard/views/dashboard.ejs,
    dashboard/routes/dashboard.js,
    .planning/phases/02-read-layer/02-CONTEXT.md
  </read_first>
  <action>
**dashboard/views/dashboard.ejs** — Replace with priority inbox. Per CONTEXT.md D-04:
- Each section: max 5 items, then "Voir tout →" link to the section page
- Overdue items: `border-l-4 border-red-500` (LEFT BORDER ACCENT only — NO `bg-red-50` fill)
- Section headers: title + count + "Voir tout" link to right

Structure:

```html
<h1 class="text-lg font-semibold text-secondary">Bienvenue, Benjamin</h1>

<!-- Health card (keep condensed grid as before) -->
<div class="mt-4 bg-white rounded-xl shadow-sm border border-gray-100 p-4">
  <!-- ... same health/counts block ... -->
</div>

<!-- INBOX-01: Posts du jour -->
<section class="mt-6">
  <div class="flex items-center justify-between mb-3">
    <h2 class="text-base font-semibold text-secondary">Posts du jour <span class="text-xs font-normal text-gray-400">(<%= todayPostsTotal %>)</span></h2>
    <% if (todayPostsTotal > 0) { %><a href="/posts" class="text-xs text-primary hover:underline">Voir tout →</a><% } %>
  </div>
  <% if (todayPosts.length === 0) { %>
    <div class="bg-white rounded-lg border border-gray-100 p-4 text-sm text-gray-400">Aucun post prevu aujourd'hui</div>
  <% } else { %>
    <div class="space-y-2">
      <% todayPosts.forEach(function(post) { %>
        <% var badgeClass = { draft:'bg-gray-100 text-gray-700', scheduled:'bg-blue-100 text-blue-700', published:'bg-green-100 text-green-700' }[post.status] || 'bg-gray-100 text-gray-500'; %>
        <div class="bg-white rounded-lg border border-gray-100 p-4 flex items-center justify-between">
          <div class="flex-1 min-w-0">
            <div class="text-sm font-medium text-secondary truncate"><%= post.hook || 'Sans titre' %></div>
            <div class="text-xs text-gray-400 mt-0.5"><%= post.platform %> · <%= post.type || '' %></div>
          </div>
          <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium <%= badgeClass %>"><%= post.status %></span>
        </div>
      <% }); %>
    </div>
  <% } %>
</section>

<!-- INBOX-02 + INBOX-04: Commentaires en attente -->
<section class="mt-6">
  <div class="flex items-center justify-between mb-3">
    <h2 class="text-base font-semibold text-secondary">
      Commentaires en attente <span class="text-xs font-normal text-gray-400">(<%= pendingCommentsTotal %>)</span>
      <% if (overdueCommentCount > 0) { %><span class="ml-1 text-xs font-normal text-red-500">— <%= overdueCommentCount %> en retard</span><% } %>
    </h2>
    <% if (pendingCommentsTotal > 0) { %><a href="/comments" class="text-xs text-primary hover:underline">Voir tout →</a><% } %>
  </div>
  <% if (pendingComments.length === 0) { %>
    <div class="bg-white rounded-lg border border-gray-100 p-4 text-sm text-gray-400">Aucun commentaire en attente</div>
  <% } else { %>
    <div class="space-y-2">
      <% pendingComments.forEach(function(c) { %>
        <!-- Per CONTEXT.md D-04: overdue = border-l-4 border-red-500, NO bg fill -->
        <div class="bg-white rounded-lg border border-gray-100 p-4 <%= c.isOverdue ? 'border-l-4 border-l-red-500' : '' %>">
          <div class="flex items-start justify-between">
            <div class="flex-1 min-w-0">
              <div class="text-sm font-medium text-secondary"><%= c.author_name || 'Anonyme' %></div>
              <div class="text-xs text-gray-400 mt-0.5">sur "<%= c.post_hook || 'Post inconnu' %>"</div>
              <div class="text-sm text-gray-600 mt-1 line-clamp-2"><%= c.comment_text %></div>
            </div>
            <% if (c.isOverdue) { %><span class="ml-3 text-xs text-red-500 font-medium">+2h</span><% } %>
          </div>
        </div>
      <% }); %>
    </div>
  <% } %>
</section>

<!-- INBOX-03: Suivis DM -->
<section class="mt-6">
  <div class="flex items-center justify-between mb-3">
    <h2 class="text-base font-semibold text-secondary">
      Suivis DM <span class="text-xs font-normal text-gray-400">(<%= activeProspectsTotal %>)</span>
      <% if (overdueProspectCount > 0) { %><span class="ml-1 text-xs font-normal text-red-500">— <%= overdueProspectCount %> en retard</span><% } %>
    </h2>
    <% if (activeProspectsTotal > 0) { %><a href="/pipeline" class="text-xs text-primary hover:underline">Voir tout →</a><% } %>
  </div>
  <% if (activeProspects.length === 0) { %>
    <div class="bg-white rounded-lg border border-gray-100 p-4 text-sm text-gray-400">Aucun suivi DM</div>
  <% } else { %>
    <div class="space-y-2">
      <% activeProspects.forEach(function(p) { %>
        <!-- Per CONTEXT.md D-04: overdue = border-l-4 border-red-500 -->
        <div class="bg-white rounded-lg border border-gray-100 p-4 <%= p.isOverdue ? 'border-l-4 border-l-red-500' : '' %>">
          <div class="flex items-center justify-between">
            <div>
              <div class="text-sm font-medium text-secondary"><%= p.prospect_name || p.full_name || 'Inconnu' %></div>
              <div class="text-xs text-gray-400 mt-0.5">Stage: <%= p.stage %> · Contact: <%= p.date_first_contact ? new Date(p.date_first_contact).toLocaleDateString('fr-FR') : '—' %></div>
            </div>
            <% if (p.isOverdue) { %><span class="text-xs text-red-500 font-medium">En retard</span><% } %>
          </div>
        </div>
      <% }); %>
    </div>
  <% } %>
</section>
```

**dashboard/tests/phase2/inbox.test.js** — Same pattern as before. Tests assert:
- "Posts du jour", "Commentaires en attente", "Suivis DM" headers present
- "Voir tout" link present when section has items
- "border-l-red-500" class applied when overdue
- Each rendered section shows max 5 items

**Rebuild Tailwind:** `cd dashboard && npm run build:css` to compile `border-l-4`, `border-l-red-500`, `line-clamp-2`.
  </action>
  <verify>
    <automated>cd "E:/Site CL/dashboard" && node -e "const fs=require('fs'); const d=fs.readFileSync('views/dashboard.ejs','utf8'); console.log('Posts du jour:', d.includes('Posts du jour')); console.log('Voir tout:', d.includes('Voir tout')); console.log('border-l-red-500:', d.includes('border-l-red-500')); console.log('NO bg-red-50:', !d.includes('bg-red-50'))" && node --test tests/phase2/inbox.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dashboard.ejs contains "Posts du jour", "Commentaires en attente", "Suivis DM"
    - dashboard.ejs contains "Voir tout" links to /posts, /comments, /pipeline
    - dashboard.ejs uses `border-l-4 border-l-red-500` for overdue (NOT `bg-red-50` fill)
    - dashboard.ejs does NOT contain `bg-red-50` or `bg-amber-50` for inbox cards
    - Each section caps display at 5 (data is pre-capped at LIMIT 5 by route)
    - inbox.test.js exists and passes
    - tailwind.css contains `border-l-red-500`
  </acceptance_criteria>
  <done>Inbox shows 3 capped sections with "Voir tout" links and red left-border accent for overdue items per D-04.</done>
</task>

</tasks>

<verification>
- `cd dashboard && node --test tests/phase2/inbox.test.js` passes
- `cd dashboard && node --test tests/*.test.js` Phase 1 still green
- Homepage shows max 5 items per section with "Voir tout →" link
- Overdue items have red left border, no red background fill
</verification>

<success_criteria>
- Sidebar nav has all 5 items as active links
- Homepage shows 3 capped (5) inbox sections
- "Voir tout →" link on each non-empty section
- Overdue style: border-l-4 border-l-red-500 only (no bg fill) per D-04
- DM timing rules from config.yaml with fallbacks
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-read-layer/02-01-SUMMARY.md`
</output>
