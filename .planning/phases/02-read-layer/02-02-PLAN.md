---
phase: 02-read-layer
plan: 02
type: execute
wave: 1
depends_on: ["01-03"]
files_modified:
  - dashboard/routes/posts.js
  - dashboard/routes/comments.js
  - dashboard/views/posts.ejs
  - dashboard/views/calendar.ejs
  - dashboard/views/comments.ejs
  - dashboard/server.js
  - dashboard/public/js/dashboard.js
  - dashboard/tests/phase2/posts.test.js
  - dashboard/tests/phase2/calendar.test.js
  - dashboard/tests/phase2/comments.test.js
  - dashboard/public/css/tailwind.css
autonomous: true
requirements:
  - POST-01
  - POST-04
  - CMT-01

must_haves:
  truths:
    - "GET /posts shows posts as a TABLE with columns Date / Titre / Statut / Nb commentaires (per CONTEXT.md D-02)"
    - "Comment count comes from a LEFT JOIN COUNT(comments) per post"
    - "GET /posts/calendar shows a MONTHLY grid only (no week toggle, per CONTEXT.md D-03)"
    - "Clicking a day in the calendar expands posts for that day below the grid (vanilla JS)"
    - "GET /comments groups by post with accordion open/close behavior (vanilla JS)"
    - "Comments use 3-state status dot: grey=unresponded, green=responded, ORANGE=pending >2h (per CONTEXT.md D-05)"
    - "Status filter preserves selection via query param ?status=draft"
  artifacts:
    - path: "dashboard/routes/posts.js"
      provides: "GET /posts (table data + comment counts via LEFT JOIN) and GET /posts/calendar (monthly only)"
      contains: "LEFT JOIN comments"
    - path: "dashboard/routes/comments.js"
      provides: "GET /comments grouped by post with age computation for orange overdue dot"
      contains: "router.get"
    - path: "dashboard/views/posts.ejs"
      provides: "Post TABLE with Date/Titre/Statut/Nb commentaires columns and status filter tabs"
      contains: "<table"
    - path: "dashboard/views/calendar.ejs"
      provides: "Monthly grid with click-to-expand day"
      contains: "data-day-key"
    - path: "dashboard/views/comments.ejs"
      provides: "Accordion-grouped comments by post with 3-state dot (grey/green/orange)"
      contains: "data-accordion"
    - path: "dashboard/public/js/dashboard.js"
      provides: "Vanilla JS for calendar day-click expand and comments accordion"
      contains: "data-accordion"
  key_links:
    - from: "dashboard/server.js"
      to: "dashboard/routes/posts.js"
      via: "app.use('/', postsRouter)"
      pattern: "require.*routes/posts"
    - from: "dashboard/server.js"
      to: "dashboard/routes/comments.js"
      via: "app.use('/', commentsRouter)"
      pattern: "require.*routes/comments"
    - from: "dashboard/views/calendar.ejs"
      to: "dashboard/public/js/dashboard.js"
      via: "<script src> + data-day-key click handlers"
      pattern: "data-day-key"
---

<objective>
Build the post list (TABLE), monthly calendar (click-to-expand day), and comments page (accordion + 3-state status dot) per locked CONTEXT.md decisions D-02, D-03, D-05.

Output: 2 route files, 3 EJS views, 1 vanilla JS file, routes mounted in server.js, integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-read-layer/02-CONTEXT.md
@.planning/phases/02-read-layer/02-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
From dashboard/lib/db.js:
```javascript
const { query } = require('../lib/db');
```

From dashboard/routes/auth.js:
```javascript
const { isAuthenticated } = require('./auth');
```

Schema columns used:
```sql
-- posts: id, status, platform, type, hook, published_date, created_date, created_at
-- comments: id, post_id, date, author_name, classification, comment_text, response_status, created_at
```

Layout expects `currentPath` for nav highlight.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Posts route (table data + comment count) + posts.ejs TABLE + monthly calendar with click-expand JS</name>
  <files>
    dashboard/routes/posts.js,
    dashboard/views/posts.ejs,
    dashboard/views/calendar.ejs,
    dashboard/public/js/dashboard.js
  </files>
  <read_first>
    dashboard/lib/db.js,
    dashboard/lib/schema.sql,
    dashboard/routes/auth.js,
    dashboard/views/layout.ejs,
    .planning/phases/02-read-layer/02-CONTEXT.md
  </read_first>
  <action>
**dashboard/routes/posts.js** — Per CONTEXT.md D-02 the post list is a TABLE with columns Date / Titre / Statut / Nb commentaires. The route MUST return `comment_count` per post via LEFT JOIN. Per D-03 the calendar is monthly only (no week toggle).

```javascript
const express = require('express');
const router = express.Router();
const { query } = require('../lib/db');
const { isAuthenticated } = require('./auth');

// POST-01: GET /posts — list as TABLE with comment counts (CONTEXT D-02)
router.get('/posts', isAuthenticated, async (req, res, next) => {
  try {
    const { status } = req.query;
    const validStatuses = ['draft', 'scheduled', 'published'];
    const filterStatus = validStatuses.includes(status) ? status : null;

    // LEFT JOIN to count comments per post
    const baseSql = `
      SELECT p.id, p.status, p.platform, p.type, p.hook, p.published_date, p.created_date, p.created_at,
             COALESCE(cc.cnt, 0)::int AS comment_count
      FROM posts p
      LEFT JOIN (
        SELECT post_id, COUNT(*)::int AS cnt FROM comments GROUP BY post_id
      ) cc ON cc.post_id = p.id
    `;
    const { rows: posts } = await query(
      filterStatus
        ? baseSql + ' WHERE p.status = $1 ORDER BY COALESCE(p.published_date, p.created_date, p.created_at) DESC'
        : baseSql + ' ORDER BY COALESCE(p.published_date, p.created_date, p.created_at) DESC',
      filterStatus ? [filterStatus] : []
    );

    const { rows: statusCounts } = await query(
      "SELECT status, COUNT(*)::int AS count FROM posts GROUP BY status"
    );
    const countMap = Object.fromEntries(statusCounts.map(r => [r.status, r.count]));

    res.render('posts', {
      currentPath: '/posts',
      posts,
      filterStatus,
      statusCounts: {
        draft: countMap.draft || 0,
        scheduled: countMap.scheduled || 0,
        published: countMap.published || 0
      },
      totalCount: (countMap.draft || 0) + (countMap.scheduled || 0) + (countMap.published || 0)
    });
  } catch (err) { next(err); }
});

// POST-04: GET /posts/calendar — MONTHLY ONLY (CONTEXT D-03)
router.get('/posts/calendar', isAuthenticated, async (req, res, next) => {
  try {
    const { date } = req.query;
    const anchor = date ? new Date(date + 'T00:00:00') : new Date();

    // Always monthly. Compute month range.
    const monthStart = new Date(anchor.getFullYear(), anchor.getMonth(), 1);
    const monthEnd = new Date(anchor.getFullYear(), anchor.getMonth() + 1, 0);

    // Pad to start of week (Monday) for grid alignment
    const startDow = monthStart.getDay(); // 0=Sun
    const padBefore = startDow === 0 ? 6 : startDow - 1;
    const gridStart = new Date(monthStart);
    gridStart.setDate(monthStart.getDate() - padBefore);

    // Pad to end of week
    const endDow = monthEnd.getDay();
    const padAfter = endDow === 0 ? 0 : 7 - endDow;
    const gridEnd = new Date(monthEnd);
    gridEnd.setDate(monthEnd.getDate() + padAfter);

    const startStr = gridStart.toISOString().split('T')[0];
    const endStr = gridEnd.toISOString().split('T')[0];

    const { rows: posts } = await query(
      'SELECT id, hook, status, platform, type, published_date FROM posts WHERE published_date BETWEEN $1 AND $2 ORDER BY published_date ASC',
      [startStr, endStr]
    );

    const postsByDate = {};
    posts.forEach(p => {
      const key = p.published_date instanceof Date
        ? p.published_date.toISOString().split('T')[0]
        : String(p.published_date);
      (postsByDate[key] = postsByDate[key] || []).push(p);
    });

    const days = [];
    const cursor = new Date(gridStart);
    while (cursor <= gridEnd) {
      days.push({
        key: cursor.toISOString().split('T')[0],
        dayNum: cursor.getDate(),
        inMonth: cursor.getMonth() === anchor.getMonth()
      });
      cursor.setDate(cursor.getDate() + 1);
    }

    const prevMonth = new Date(anchor.getFullYear(), anchor.getMonth() - 1, 1);
    const nextMonth = new Date(anchor.getFullYear(), anchor.getMonth() + 1, 1);

    res.render('calendar', {
      currentPath: '/posts/calendar',
      days,
      postsByDate,
      monthLabel: anchor.toLocaleDateString('fr-FR', { month: 'long', year: 'numeric' }),
      prevDateStr: prevMonth.toISOString().split('T')[0],
      nextDateStr: nextMonth.toISOString().split('T')[0]
    });
  } catch (err) { next(err); }
});

module.exports = router;
```

**dashboard/views/posts.ejs** — TABLE layout (CONTEXT D-02):

```html
<div class="max-w-5xl">
  <h1 class="text-lg font-semibold text-secondary">Posts</h1>

  <div class="mt-4 flex items-center gap-3">
    <a href="/posts" class="text-sm font-medium text-primary border-b-2 border-primary pb-1">Liste</a>
    <a href="/posts/calendar" class="text-sm font-medium text-gray-400 hover:text-gray-600 pb-1">Calendrier</a>
  </div>

  <!-- Status filter tabs -->
  <div class="mt-4 flex gap-2 flex-wrap">
    <a href="/posts" class="px-3 py-1.5 rounded-full text-xs font-medium <%= !filterStatus ? 'bg-secondary text-white' : 'bg-gray-100 text-gray-600' %>">Tous (<%= totalCount %>)</a>
    <a href="/posts?status=draft" class="px-3 py-1.5 rounded-full text-xs font-medium <%= filterStatus==='draft' ? 'bg-secondary text-white' : 'bg-gray-100 text-gray-600' %>">Brouillons (<%= statusCounts.draft %>)</a>
    <a href="/posts?status=scheduled" class="px-3 py-1.5 rounded-full text-xs font-medium <%= filterStatus==='scheduled' ? 'bg-secondary text-white' : 'bg-gray-100 text-gray-600' %>">Programmes (<%= statusCounts.scheduled %>)</a>
    <a href="/posts?status=published" class="px-3 py-1.5 rounded-full text-xs font-medium <%= filterStatus==='published' ? 'bg-secondary text-white' : 'bg-gray-100 text-gray-600' %>">Publies (<%= statusCounts.published %>)</a>
  </div>

  <!-- TABLE: Date / Titre / Statut / Nb commentaires (CONTEXT D-02) -->
  <div class="mt-4 bg-white rounded-xl border border-gray-100 overflow-hidden">
    <table class="w-full text-sm">
      <thead class="bg-gray-50 text-xs uppercase text-gray-500">
        <tr>
          <th class="text-left px-4 py-3 w-32">Date</th>
          <th class="text-left px-4 py-3">Titre</th>
          <th class="text-left px-4 py-3 w-32">Statut</th>
          <th class="text-right px-4 py-3 w-32">Nb commentaires</th>
        </tr>
      </thead>
      <tbody class="divide-y divide-gray-100">
        <% if (posts.length === 0) { %>
          <tr><td colspan="4" class="px-4 py-6 text-center text-gray-400">Aucun post trouve</td></tr>
        <% } else { %>
          <% posts.forEach(function(post) { %>
            <% var badgeClass = { draft:'bg-gray-100 text-gray-700', scheduled:'bg-blue-100 text-blue-700', published:'bg-green-100 text-green-700' }[post.status] || 'bg-gray-100 text-gray-500'; %>
            <tr class="hover:bg-gray-50">
              <td class="px-4 py-3 text-xs text-gray-500"><%= post.published_date ? new Date(post.published_date).toLocaleDateString('fr-FR') : '—' %></td>
              <td class="px-4 py-3 text-secondary"><%= post.hook || 'Sans titre' %></td>
              <td class="px-4 py-3"><span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium <%= badgeClass %>"><%= post.status %></span></td>
              <td class="px-4 py-3 text-right text-secondary font-medium"><%= post.comment_count %></td>
            </tr>
          <% }); %>
        <% } %>
      </tbody>
    </table>
  </div>
</div>
```

**dashboard/views/calendar.ejs** — Monthly only, click-to-expand day (CONTEXT D-03):

```html
<div class="max-w-5xl">
  <h1 class="text-lg font-semibold text-secondary">Calendrier</h1>

  <div class="mt-4 flex items-center gap-3">
    <a href="/posts" class="text-sm font-medium text-gray-400 hover:text-gray-600 pb-1">Liste</a>
    <a href="/posts/calendar" class="text-sm font-medium text-primary border-b-2 border-primary pb-1">Calendrier</a>
  </div>

  <div class="mt-4 flex items-center justify-between">
    <span class="text-sm font-medium text-secondary capitalize"><%= monthLabel %></span>
    <div class="flex items-center gap-2">
      <a href="/posts/calendar?date=<%= prevDateStr %>" class="px-2 py-1 rounded bg-gray-100 text-gray-600 text-xs hover:bg-gray-200">&larr;</a>
      <a href="/posts/calendar?date=<%= nextDateStr %>" class="px-2 py-1 rounded bg-gray-100 text-gray-600 text-xs hover:bg-gray-200">&rarr;</a>
    </div>
  </div>

  <!-- Monthly grid -->
  <div class="mt-4 grid grid-cols-7 gap-1">
    <% ['Lun','Mar','Mer','Jeu','Ven','Sam','Dim'].forEach(function(d) { %>
      <div class="text-center text-xs font-medium text-gray-500 py-1"><%= d %></div>
    <% }); %>

    <% days.forEach(function(day) { %>
      <% var isToday = day.key === new Date().toISOString().split('T')[0]; %>
      <% var cellPosts = postsByDate[day.key] || []; %>
      <button type="button"
              data-day-key="<%= day.key %>"
              class="text-left min-h-20 bg-white rounded-lg p-2 border <%= isToday ? 'border-primary' : 'border-gray-100' %> <%= day.inMonth ? '' : 'opacity-40' %> hover:border-primary cursor-pointer">
        <div class="text-xs font-medium <%= isToday ? 'text-primary' : 'text-gray-500' %> mb-1"><%= day.dayNum %></div>
        <% cellPosts.slice(0,3).forEach(function(post) { %>
          <% var dotColor = { draft:'bg-gray-400', scheduled:'bg-blue-500', published:'bg-green-500' }[post.status] || 'bg-gray-400'; %>
          <div class="text-xs flex items-center gap-1 mb-0.5 truncate">
            <span class="w-1.5 h-1.5 rounded-full flex-shrink-0 <%= dotColor %>"></span>
            <span class="truncate"><%= post.hook ? post.hook.substring(0,18) : '—' %></span>
          </div>
        <% }); %>
        <% if (cellPosts.length > 3) { %>
          <div class="text-xs text-gray-400">+<%= cellPosts.length - 3 %></div>
        <% } %>
      </button>
    <% }); %>
  </div>

  <!-- Click-to-expand day panel (filled by JS) -->
  <div id="day-expand-panel" class="mt-4 hidden bg-white rounded-xl border border-gray-100 p-4">
    <div class="flex items-center justify-between mb-3">
      <h2 id="day-expand-title" class="text-sm font-semibold text-secondary"></h2>
      <button type="button" id="day-expand-close" class="text-xs text-gray-400 hover:text-gray-600">Fermer ✕</button>
    </div>
    <div id="day-expand-list" class="space-y-2"></div>
  </div>

  <!-- Inline data for JS -->
  <script id="calendar-data" type="application/json"><%- JSON.stringify(postsByDate) %></script>
  <script src="/js/dashboard.js" defer></script>
</div>
```

**dashboard/public/js/dashboard.js** — Vanilla JS for calendar day-click and comments accordion:

```javascript
(function() {
  // Calendar: click day -> expand panel below
  const dataEl = document.getElementById('calendar-data');
  const panel = document.getElementById('day-expand-panel');
  const title = document.getElementById('day-expand-title');
  const list = document.getElementById('day-expand-list');
  const closeBtn = document.getElementById('day-expand-close');

  if (dataEl && panel) {
    const postsByDate = JSON.parse(dataEl.textContent || '{}');
    document.querySelectorAll('[data-day-key]').forEach(btn => {
      btn.addEventListener('click', () => {
        const key = btn.getAttribute('data-day-key');
        const items = postsByDate[key] || [];
        title.textContent = new Date(key + 'T00:00:00').toLocaleDateString('fr-FR', { weekday:'long', day:'numeric', month:'long', year:'numeric' });
        list.innerHTML = items.length === 0
          ? '<div class="text-sm text-gray-400">Aucun post ce jour</div>'
          : items.map(p => {
              const badge = { draft:'bg-gray-100 text-gray-700', scheduled:'bg-blue-100 text-blue-700', published:'bg-green-100 text-green-700' }[p.status] || 'bg-gray-100';
              return `<div class="border border-gray-100 rounded-lg p-3 flex items-center justify-between">
                <div class="text-sm text-secondary">${(p.hook || 'Sans titre').replace(/[<>&]/g,'')}</div>
                <span class="px-2 py-0.5 rounded text-xs font-medium ${badge}">${p.status}</span>
              </div>`;
            }).join('');
        panel.classList.remove('hidden');
        panel.scrollIntoView({ behavior:'smooth', block:'nearest' });
      });
    });
    if (closeBtn) closeBtn.addEventListener('click', () => panel.classList.add('hidden'));
  }

  // Comments accordion: toggle open/close
  document.querySelectorAll('[data-accordion]').forEach(header => {
    header.addEventListener('click', () => {
      const id = header.getAttribute('data-accordion');
      const body = document.getElementById('accordion-body-' + id);
      const icon = header.querySelector('[data-accordion-icon]');
      if (!body) return;
      const open = body.classList.toggle('hidden');
      if (icon) icon.textContent = open ? '▸' : '▾';
    });
  });
})();
```
  </action>
  <verify>
    <automated>cd "E:/Site CL/dashboard" && node -e "const fs=require('fs'); const r=fs.readFileSync('routes/posts.js','utf8'); console.log('LEFT JOIN:', r.includes('LEFT JOIN')); console.log('comment_count:', r.includes('comment_count')); const p=fs.readFileSync('views/posts.ejs','utf8'); console.log('table:', p.includes('<table')); console.log('Nb commentaires:', p.includes('Nb commentaires')); const c=fs.readFileSync('views/calendar.ejs','utf8'); console.log('data-day-key:', c.includes('data-day-key')); console.log('NO week toggle:', !c.includes('view=week')); const j=fs.readFileSync('public/js/dashboard.js','utf8'); console.log('day-expand:', j.includes('day-expand-panel'))"</automated>
  </verify>
  <acceptance_criteria>
    - routes/posts.js contains `LEFT JOIN` and `comment_count`
    - routes/posts.js calendar handler has NO `view=week` and NO week/month toggle logic
    - views/posts.ejs contains `<table` and `Nb commentaires` and `Date` `Titre` `Statut` headers
    - views/posts.ejs does NOT contain card-style `space-y-2` post cards (it's a table now)
    - views/calendar.ejs contains `data-day-key` and `id="day-expand-panel"`
    - views/calendar.ejs does NOT contain `view === 'week'` or `Semaine` toggle
    - public/js/dashboard.js contains day-expand and accordion handlers
  </acceptance_criteria>
  <done>Posts list is a table with comment counts; calendar is monthly-only with click-to-expand day panel.</done>
</task>

<task type="auto">
  <name>Task 2: Comments route + accordion view with 3-state dot (grey/green/orange) + mount routes + tests</name>
  <files>
    dashboard/routes/comments.js,
    dashboard/views/comments.ejs,
    dashboard/server.js,
    dashboard/tests/phase2/posts.test.js,
    dashboard/tests/phase2/calendar.test.js,
    dashboard/tests/phase2/comments.test.js,
    dashboard/public/css/tailwind.css
  </files>
  <read_first>
    dashboard/server.js,
    dashboard/lib/db.js,
    dashboard/routes/auth.js,
    dashboard/routes/posts.js,
    .planning/phases/02-read-layer/02-CONTEXT.md
  </read_first>
  <action>
**dashboard/routes/comments.js** — Group by post + compute orange-overdue flag (pending >2h, per CONTEXT D-05):

```javascript
const express = require('express');
const router = express.Router();
const { query } = require('../lib/db');
const { isAuthenticated } = require('./auth');

router.get('/comments', isAuthenticated, async (req, res, next) => {
  try {
    const { rows: comments } = await query(`
      SELECT c.*, p.hook AS post_hook, p.status AS post_status
      FROM comments c
      LEFT JOIN posts p ON c.post_id = p.id
      ORDER BY c.post_id ASC, c.date ASC
    `);

    const TWO_HOURS_MS = 2 * 60 * 60 * 1000;
    const now = Date.now();

    // CONTEXT D-05: 3-state dot
    //   grey   = unresponded (pending, age <= 2h)
    //   orange = pending and age > 2h
    //   green  = responded
    const withDotState = comments.map(c => {
      let dotState = 'grey';
      if (c.response_status && c.response_status !== 'pending') {
        dotState = 'green';
      } else {
        const ageMs = now - new Date(c.created_at || c.date).getTime();
        dotState = ageMs > TWO_HOURS_MS ? 'orange' : 'grey';
      }
      return { ...c, dotState };
    });

    const byPost = {};
    withDotState.forEach(c => {
      const pid = c.post_id || '_orphan';
      if (!byPost[pid]) {
        byPost[pid] = { post_id: c.post_id, post_hook: c.post_hook || 'Post inconnu', post_status: c.post_status, comments: [], pendingCount: 0, overdueCount: 0 };
      }
      byPost[pid].comments.push(c);
      if (c.dotState !== 'green') byPost[pid].pendingCount++;
      if (c.dotState === 'orange') byPost[pid].overdueCount++;
    });

    res.render('comments', {
      currentPath: '/comments',
      byPost,
      totalCount: comments.length,
      pendingCount: withDotState.filter(c => c.dotState !== 'green').length,
      overdueCount: withDotState.filter(c => c.dotState === 'orange').length
    });
  } catch (err) { next(err); }
});

module.exports = router;
```

**dashboard/views/comments.ejs** — Accordion (CONTEXT D-05) + 3-state dot:

```html
<div class="max-w-4xl">
  <h1 class="text-lg font-semibold text-secondary">Commentaires</h1>
  <div class="mt-2 text-sm text-gray-500">
    <%= totalCount %> commentaires · <%= pendingCount %> en attente
    <% if (overdueCount > 0) { %><span class="text-orange-600">· <%= overdueCount %> en retard (>2h)</span><% } %>
  </div>

  <div class="mt-4 space-y-3">
    <% Object.keys(byPost).forEach(function(postId, idx) { %>
      <% var group = byPost[postId]; %>
      <div class="bg-white rounded-xl border border-gray-100 overflow-hidden">
        <!-- Accordion header (clickable) -->
        <button type="button"
                data-accordion="<%= idx %>"
                class="w-full flex items-center justify-between px-4 py-3 bg-gray-50 hover:bg-gray-100 text-left">
          <div>
            <div class="text-sm font-medium text-secondary"><%= group.post_hook %></div>
            <div class="text-xs text-gray-400 mt-0.5">
              <%= group.comments.length %> commentaire<%= group.comments.length>1?'s':'' %>
              <% if (group.pendingCount > 0) { %> · <%= group.pendingCount %> en attente<% } %>
              <% if (group.overdueCount > 0) { %> · <span class="text-orange-600"><%= group.overdueCount %> en retard</span><% } %>
            </div>
          </div>
          <span data-accordion-icon class="text-gray-400">▾</span>
        </button>
        <!-- Accordion body -->
        <div id="accordion-body-<%= idx %>" class="divide-y divide-gray-50">
          <% group.comments.forEach(function(c) { %>
            <%
              // CONTEXT D-05: grey/green/orange dot
              var dotColor = { grey:'bg-gray-400', orange:'bg-orange-500', green:'bg-green-500' }[c.dotState];
              var classifBadge = { info:'bg-blue-100 text-blue-700', objection:'bg-amber-100 text-amber-700', positive:'bg-green-100 text-green-700' }[c.classification] || 'bg-gray-100 text-gray-500';
            %>
            <div class="px-4 py-3">
              <div class="flex items-start justify-between">
                <div class="flex-1 min-w-0">
                  <div class="flex items-center gap-2">
                    <span class="w-2.5 h-2.5 rounded-full flex-shrink-0 <%= dotColor %>" title="<%= c.dotState %>"></span>
                    <span class="text-sm font-medium text-secondary"><%= c.author_name || 'Anonyme' %></span>
                    <span class="text-xs text-gray-400"><%= c.date ? new Date(c.date).toLocaleDateString('fr-FR') : '' %></span>
                  </div>
                  <div class="text-sm text-gray-600 mt-1 ml-4"><%= c.comment_text %></div>
                </div>
                <span class="ml-3 inline-flex items-center px-2 py-0.5 rounded text-xs font-medium <%= classifBadge %>"><%= c.classification || 'non classe' %></span>
              </div>
            </div>
          <% }); %>
        </div>
      </div>
    <% }); %>
    <% if (Object.keys(byPost).length === 0) { %>
      <div class="bg-white rounded-lg border border-gray-100 p-6 text-sm text-gray-400 text-center">Aucun commentaire</div>
    <% } %>
  </div>

  <script src="/js/dashboard.js" defer></script>
</div>
```

**dashboard/server.js** — Mount postsRouter and commentsRouter (append-only). Plan 03 will add pipeline + stats.

```javascript
const postsRouter = require('./routes/posts');
const commentsRouter = require('./routes/comments');
// after app.use('/', dashRouter):
app.use('/', postsRouter);
app.use('/', commentsRouter);
```

**Tests** — pattern same as Phase 1 auth.test.js. Assertions:

- `posts.test.js`: GET /posts → 200, body contains `<table` and `Nb commentaires`; `?status=draft` filters; auth redirect.
- `calendar.test.js`: GET /posts/calendar → 200, body contains `data-day-key`; does NOT contain `Semaine` or `view=week`.
- `comments.test.js`: GET /comments → 200, body contains `data-accordion`; auth redirect.

**Rebuild Tailwind:** `cd dashboard && npm run build:css` (compiles `bg-orange-500`, `text-orange-600`).
  </action>
  <verify>
    <automated>cd "E:/Site CL/dashboard" && node -e "const fs=require('fs'); const r=fs.readFileSync('routes/comments.js','utf8'); console.log('dotState:', r.includes('dotState')); console.log('orange:', r.includes(\"'orange'\")); const v=fs.readFileSync('views/comments.ejs','utf8'); console.log('accordion:', v.includes('data-accordion')); console.log('orange-500:', v.includes('orange-500')); const s=fs.readFileSync('server.js','utf8'); console.log('mounts posts:', s.includes('routes/posts')); console.log('mounts comments:', s.includes('routes/comments'))" && node --test tests/phase2/posts.test.js tests/phase2/calendar.test.js tests/phase2/comments.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - routes/comments.js computes `dotState` with values `grey`, `orange`, `green`
    - routes/comments.js uses `TWO_HOURS_MS` threshold for orange state (CONTEXT D-05)
    - views/comments.ejs contains `data-accordion` and `accordion-body-`
    - views/comments.ejs uses `bg-orange-500` for orange dot state
    - server.js mounts postsRouter and commentsRouter
    - All 3 phase2 tests pass
    - tailwind.css contains `bg-orange-500`
  </acceptance_criteria>
  <done>Comments page uses accordion + 3-state dot (grey/green/orange) per CONTEXT D-05. Routes mounted. Tests green.</done>
</task>

</tasks>

<verification>
- `cd dashboard && node --test tests/phase2/posts.test.js tests/phase2/calendar.test.js tests/phase2/comments.test.js` passes
- Posts page renders a `<table>` with Date / Titre / Statut / Nb commentaires
- Calendar is monthly only; clicking a day expands a panel with that day's posts
- Comments use accordion; pending comments >2h show orange dot
</verification>

<success_criteria>
- Post list is TABLE with 4 columns including comment count via LEFT JOIN
- Calendar is monthly-only with click-to-expand day (no week toggle)
- Comments use accordion + 3-state dot (grey/green/orange)
- All routes auth-protected, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-read-layer/02-02-SUMMARY.md`
</output>
